/**
 * Variáveis de ambiente para o modo de PRODUÇÃO
 * Substitua a apiUrl pela URL real do seu servidor na AWS
 */
export const environment = {
  producao: true,
  apiUrl: 'https://sua-api.elasticbeanstalk.com/api',
};
